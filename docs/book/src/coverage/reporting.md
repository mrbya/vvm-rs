# Reporting

After merging, VVM can render:

- merged JSON for machine consumption;
- text for CI logs and quick review;
- self-contained HTML for browser inspection;
- a GitLab metric line for regex-based coverage extraction.

The reporting surface exists to explain what was covered, not just to print one
percentage. Bin-level detail controls let you trade verbosity for focus.
