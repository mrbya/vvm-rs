# Installation

Install the binary with Cargo:

```bash
cargo install cargo-vvm
```

During repository development you can also run it from source with:

```bash
cargo run -p cargo-vvm -- coverage --help
```
