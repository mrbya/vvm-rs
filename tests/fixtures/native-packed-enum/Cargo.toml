[package]
name = "vvm-native-fixture-packed-enum"
version = "0.0.0"
edition = "2024"
rust-version = "1.87"
publish = false
build = "build.rs"
description = "VVM packed-enum verification example"

[dev-dependencies]
vvm = { package = "vvm-rs", path = "__VVM_PATH__" }

[build-dependencies]
vvm-build = { path = "__VVM_BUILD_PATH__" }

[workspace]
